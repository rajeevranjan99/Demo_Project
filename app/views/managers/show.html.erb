<h1>Show the managed_flight</h1>
<h1>Manager Details</h1>

<p><strong>First_Name:</strong> <%= @manager.first_name %></p>
<p><strong>Last_Name:</strong> <%= @manager.last_name %></p> 
<p><strong>Phone_Number:</strong> <%= @manager.phone_number%></p>

<li><%= link_to 'Edit', edit_manager_path(@manager) %> </li>
<li><%= link_to "Destroy", managers_path(@manager), data: {
                    turbo_method: :delete,
                    turbo_confirm: "Are you sure ?"
                  } %></li>
<li><%= link_to 'Back', managers_path %></li>